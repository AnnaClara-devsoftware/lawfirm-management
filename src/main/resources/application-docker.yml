spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:lawfirm_db}
    username: ${DB_USER:lawfirm_user}
    password: ${DB_PASSWORD:changeme}
